# Solana devnet RPC endpoint
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com

# Deployed commitment program
NEXT_PUBLIC_PROGRAM_ID=3uyiF93zMvUcP2o1Cqnt2iS4bXwYeBcTMTvbaTf5B3RJ

# Indexer / keeper base URLs
# Local split backends:
#   NEXT_PUBLIC_INDEXER_URL=http://localhost:3002
#   NEXT_PUBLIC_KEEPER_URL=http://localhost:3001
# Vercel: leave BOTH unset — the Next.js /api/* BFF serves the same routes
# (on-chain board + TxLINE scores) so the site works without Railway.
NEXT_PUBLIC_INDEXER_URL=http://localhost:3002
NEXT_PUBLIC_KEEPER_URL=http://localhost:3001

# --- Server-only (Vercel Environment Variables; never NEXT_PUBLIC_) ---
# Required on Vercel for match scores / live TxLINE data:
# TXLINE_API_TOKEN=txoracle_api_...
# Optional (auto-refreshed via guest auth if omitted):
# TXLINE_JWT=
